import React from 'react';
import ReactDOM from 'react-dom/client';
import TreasureHuntApp from './treasure-hunt-app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TreasureHuntApp />
  </React.StrictMode>
);
